@AGENTS.md

# วิธีทำงานกับโปรเจกต์นี้

มาตรฐานเต็มอยู่ที่ [standard/v1.md](standard/v1.md) — ข้างล่างนี้คือสิ่งที่ต้องทำทุกครั้ง

## คำสั่งที่ต้องรู้

| คำสั่ง | ใช้เมื่อไหร่ |
|---|---|
| `./gate.sh` | ก่อน push (pre-push เรียกให้เอง) — tsc เท่านั้น ~12-16 วิ |
| `./gate.sh --full` | สิ่งที่ CI รัน — tsc + lint ratchet + build + render smoke |
| `node scripts/scope.mjs <ชื่อ>` | **ก่อนแก้อะไรก็ตาม** — ดูว่ากระทบที่ไหนบ้าง (~20 วิ ไม่ใช่ gate) |
| `node scripts/ratchet.mjs --update` | หลังแก้ปัญหา lint เก่าได้แล้ว |

ตั้ง hook ครั้งเดียวต่อเครื่อง: `git config core.hooksPath .githooks`

## ขอบเขตการแก้ — กติกาบังคับตัวเอง

1. **รัน `scope.mjs` ก่อนลงมือ** ผลที่ได้คือขอบเขตที่ถูกต้อง ห้ามเดาเอง ห้ามไล่อ่านทั้งโปรเจกต์แทน
2. **แก้เฉพาะไฟล์ที่ถูกระบุ** ห้าม refactor ของข้างเคียงที่ไม่ได้สั่ง
3. **เจอของที่ควรแก้แต่อยู่นอกขอบเขต → รายงาน ไม่ใช่แก้** เขียนท้ายคำตอบว่าเจออะไร อยู่ไหน ใช้อยู่กี่ที่ แล้วให้คนตัดสิน
4. **แก้แบบเพิ่ม ไม่ทับของเดิม** ของใหม่หรือของที่เสี่ยงให้อยู่หลัง flag
5. `scope.mjs` เขียว ≠ ปลอดภัยครบ — มันไม่เห็น logic ที่ก๊อปซ้ำ, string key, coupling ข้ามขอบเขต

## หน้าที่จด gaps

**ถ้า gate เขียวแล้วยังพัง → เติม 1 บรรทัดใน [standard/gaps.md](standard/gaps.md) ก่อนปิดงาน**

นี่ไม่ใช่พิธีกรรม — เป็นตัวนับที่ใช้ตัดสินว่าจะเปิดใช้เครื่องมือที่เลื่อนไว้เมื่อไหร่
ถ้าไม่จด หลักฐานจะไม่สะสม แล้วของที่ "รอหลักฐาน" จะไม่มีวันถูกเปิด

## กฎ migration (ใช้เมื่อมี DB แล้ว)

ให้ **โค้ดเวอร์ชันเก่ายังรันได้กับ schema ใหม่เสมอ** — ถ้าทำได้ การถอยจะเหลือแค่ revert โค้ด

| โค้ดเก่ายังรันได้ → ไม่ต้องถอย schema | ห้ามทำใน deploy เดียว |
|---|---|
| เพิ่มตาราง / เพิ่มคอลัมน์ nullable | เปลี่ยนชื่อคอลัมน์ |
| เพิ่ม index | ลบคอลัมน์ / ลบตาราง |
| เพิ่มค่า enum ⚠️ ประตูทางเดียว ถอยไม่ได้ | เปลี่ยน type ของคอลัมน์ |
| | เพิ่ม NOT NULL โดยไม่มี default |

ของคอลัมน์ขวาต้องซอยเป็น 3 deploy: เพิ่มของใหม่ → เขียนทั้งสองที่ + ย้ายข้อมูล → ค่อยลบของเก่า

กับดัก enum: **ทุก switch ที่อ่าน enum จาก DB ต้องมี `default`** ไม่งั้นค่าใหม่จะทำให้โค้ดเก่าตาย ·
และ **ห้ามเพิ่มค่า enum แล้ว UPDATE ไปใช้ค่านั้นใน migration เดียวกัน** Postgres ไม่อนุญาต

⚠️ ห้ามรัน `supabase db reset` กับ production เด็ดขาด — มันล้างข้อมูล
