<!-- standard: v1.0 (ทาง A — JavaScript) — https://github.com/MazDev01/dev-standard -->

# วิธีทำงานกับโปรเจกต์นี้

โปรเจกต์นี้เป็น **Next.js + JavaScript** (ไม่ใช่ TypeScript)

## คำสั่ง

| คำสั่ง | ใช้เมื่อไหร่ |
|---|---|
| `./gate.sh --full` | ก่อน push — build + render smoke (pre-push เรียกให้เอง) |
| `./gate.sh` | ชั้นเร็ว — **ตอนนี้ยังไม่มีอะไรให้ตรวจ** ดูเหตุผลใน `gate.sh` |

ตั้ง hook ครั้งเดียวต่อเครื่อง: `git config core.hooksPath .githooks`

route ที่ smoke ตรวจอยู่ใน `standard/config.json`

## ⚠️ โปรเจกต์นี้ไม่มีเครื่องมือหารัศมีผลกระทบอัตโนมัติ

โปรเจกต์ TypeScript ของทีมใช้ `scripts/scope.mjs` ตอบว่า "แก้ตรงนี้กระทบที่ไหนบ้าง" ได้ด้วยคำสั่งเดียว
**แต่มันต้องพึ่ง type จึงใช้กับโปรเจกต์นี้ไม่ได้**

เพราะฉะนั้นก่อนแก้อะไร ให้หาด้วยมือก่อน:

1. **ค้นหาชื่อฟังก์ชัน/คอมโพเนนต์ทั้งโปรเจกต์** (`grep` หรือ Find in Files)
2. ในเอดิเตอร์ใช้ **Find All References** (คลิกขวาที่ชื่อ) — ได้ผลบางส่วน เพราะ JS ไม่มี type
3. ดูรายการที่ได้ แล้ว**แก้เฉพาะจุดในรายการนั้น**

⚠️ วิธีนี้ไม่ครบเท่าเครื่องมือ — มันจับ **ชื่อที่ตรงกันเท่านั้น** ไม่เห็น:
- logic เดียวกันที่ถูกเขียนซ้ำไว้ที่อื่นด้วยชื่ออื่น
- การเรียกผ่าน string / dynamic key
- coupling ที่เกิดตอนรัน

**ห้ามไล่อ่านทั้งโปรเจกต์แทน** — ค้นหาชื่อก่อนเสมอ นั่นคือเหตุผลทั้งหมดที่กฎนี้มีอยู่

## ขอบเขตการแก้ — กติกาบังคับตัวเอง

1. **ค้นหาก่อนลงมือ** ผลที่ได้คือขอบเขต ห้ามเดา ห้ามอ่านทั้งโปรเจกต์
2. **แก้เฉพาะไฟล์ที่ถูกระบุ** ห้าม refactor ของข้างเคียงที่ไม่ได้สั่ง
3. **เจอของที่ควรแก้แต่อยู่นอกขอบเขต → รายงาน ไม่ใช่แก้** เขียนท้ายคำตอบว่าเจออะไร อยู่ไหน ใช้อยู่กี่ที่ แล้วให้คนตัดสิน
4. **แก้แบบเพิ่ม / backward-compatible** ไม่ทับของเดิม ของที่เสี่ยงให้อยู่หลัง flag
5. **ก่อนบอกว่าเสร็จ ต้องผ่าน `./gate.sh --full`** แล้วรายงานผลจริง ห้ามเดา ห้ามบอกว่า "น่าจะใช้ได้"

## หน้าที่จด gaps

**ถ้า gate เขียวแล้วยังพัง → เติม 1 บรรทัดใน [standard/gaps.md](standard/gaps.md) ก่อนปิดงาน**

เป็นตัวนับหลักฐานที่ใช้ตัดสินว่าจะเพิ่มเครื่องมืออะไรต่อ ถ้าไม่จด หลักฐานจะไม่สะสม

## ความปลอดภัย

- โปรเจกต์นี้ยังไม่มีฐานข้อมูล (ใช้ mock ใน `app/lib/`) — แก้โค้ดไม่กระทบข้อมูลจริง
- `smoke.mjs` **เปิดแอปขึ้นมาจริง** ถ้าวันหน้าต่อ DB แล้ว ต้องไม่ให้ `.env` ชี้ไป production
- ห้ามใส่ credential ของ production ใน CI secrets
