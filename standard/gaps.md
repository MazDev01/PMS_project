# gaps — เคสที่ gate เขียวแล้วยังพัง

**กติกา:** gate ผ่านแต่ของยังพัง → เติม 1 บรรทัดที่นี่ก่อนปิดงาน (AI เป็นคนเติม ทีมไม่ต้องจำ)

ไฟล์นี้คือ**ตัวนับหลักฐาน** ที่ใช้ตัดสินว่าจะเปิดเครื่องมือที่เลื่อนไว้เมื่อไหร่
ถ้าไม่มีที่จด ครั้งที่ 1 กับครั้งที่ 2 จะไม่มีวันถูกนับต่อกัน แล้ว "เลื่อนไว้" จะกลายเป็น "ตัดทิ้ง" โดยปริยาย

รูปแบบ: `วันที่ | หมวด | อาการ | ที่เกิด`

หมวด: `dup` (logic ซ้ำ) · `string-key` (typo ใน rpc/flag/event/route) · `perf` · `seam` (coupling ข้ามขอบเขต) · `flaky` · `other`

## นับตอนนี้

| หมวด | จำนวน | เปิดใช้เมื่อ |
|---|---|---|
| dup | 0 | ถึง 2 → เปิด clone detector |
| string-key | 0 | ถึง 1 → เปิด const registry + lint rule |
| seam | 0 | ถึง 1 → เริ่มออกแบบ contract รอยต่อโมดูล (R4) |
| **flaky** | **1** | ถึง 3 → ทบทวนว่า gate เข้มเกินไปตรงไหน |
| perf | — | **ไม่รอให้พัง** ดู v1.md — perf ต้องวัดก่อนส่งมอบ |

## บันทึก

```
2026-08-14 | flaky | CI แดง 2 รอบติด แต่ route ที่พังเปลี่ยนทุกรอบ
                     (/admin/dashboard → /contractor/login)
                     สาเหตุ: ฟอนต์ Noto Sans Thai จาก fonts.gstatic.com คืน 404 เป็นครั้งคราว
                     ไม่ใช่บั๊กในแอป — เป็นของภายนอก
                     แก้ที่ dev-standard v1.6: ของภายนอกไม่ทำให้ gate แดง แต่ยังรายงานให้เห็น
                     | CI (Ubuntu) เท่านั้น · เครื่อง Windows ไม่เคยเจอ
```

## ของค้างที่ยังไม่แก้ (ไม่ใช่ gap แต่ควรเห็น)

**แอปโหลดฟอนต์จาก Google CDN ตอนรัน** — `@next/next/no-page-custom-font` เตือนอยู่ในเลขฐานแล้ว 1 จุด

ถ้าเปลี่ยนไปใช้ `next/font/google` ฟอนต์จะถูก self-host ตอน build
→ หน้าเร็วขึ้น · ไม่พึ่ง CDN คนอื่น · flaky ข้างบนหายถาวร

เป็นการแก้โค้ดแอป ควรเป็นงานแยก — **และเหมาะจะเป็นงานหนึ่งในการวัด `/cost`** เพราะเป็นงานจริงที่มีเหตุผลรองรับ
